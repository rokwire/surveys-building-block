type: object
properties:
  start_time_before:
    type: integer
    nullable: true
    format: int64
    description: "UNIX UTC timestamp in seconds"
  start_time_after:
    type: integer
    nullable: true
    format: int64
    description: "UNIX UTC timestamp in seconds"
  end_time_before:
    type: integer
    nullable: true
    format: int64
    description: "UNIX UTC timestamp in seconds"
  end_time_after:
    type: integer
    nullable: true
    format: int64
    description: "UNIX UTC timestamp in seconds"